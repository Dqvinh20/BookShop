<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="BookShop.Custom.ProductCountControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookShop.Custom"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Horizontal">
        <TextBlock
            VerticalAlignment="Center"
            Text="Products per page: " />
        <NumberBox
            NumberFormatter="{x:Bind NumberFormatter,Mode=OneWay}"
            VerticalAlignment="Center"
            Maximum="{x:Bind MaxProductCount, Mode=OneWay}"
            Minimum="{x:Bind MinProductCount, Mode=OneWay}"
            ValueChanged="CurrentCountNumberBox_ValueChanged"
            Value="{x:Bind CurrentProductCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</UserControl>
